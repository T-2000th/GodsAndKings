#events for ROADS
namespace = mr_roads

country_event = {
	id = mr_roads.1
	title = mr_roads.1.t
	desc = mr_roads.1.d
	picture = MERCHANTS_TALKING_eventPicture

	is_triggered_only = yes
	trigger = {
		#testing
	}

	immediate = {
		pathfinder_effect = yes
	}

	option = {
		name = mr_roads.1
		every_owned_province = {
			limit = {
				has_province_flag = pathfinding_P
			}
			add_base_tax = 100
		}
	}
	
	after = {
		every_owned_province = {
			clr_province_flag = pathfinding_P
		}
	}
}